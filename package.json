{
  "name": "comparador-precos-nexus",
  "version": "0.1.0",
  "private": true,
  "description": "Painel de comparacao de precos para jogos digitais com licenca primaria e secundaria.",
  "scripts": {
    "start": "netlify dev",
    "check": "node --check netlify/functions/refresh-prices.js && node --check worker/daily-refresh.js && node --check app.js",
    "worker:refresh": "node worker/daily-refresh.js"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.8"
  },
  "engines": {
    "node": ">=18"
  }
}
