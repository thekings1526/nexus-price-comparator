services:
  - type: cron
    name: nexus-price-worker
    runtime: node
    plan: starter
    schedule: "0 8 1 1 *"
    buildCommand: "npm install"
    startCommand: "npm run worker:refresh"
    envVars:
      - key: NEXUS_BLOBS_SITE_ID
        sync: false
      - key: NEXUS_BLOBS_TOKEN
        sync: false
      - key: WORKER_BATCH_SIZE
        value: "1"
      - key: WORKER_REQUEST_DELAY_MS
        value: "1200"
      - key: WORKER_ITEM_RETRIES
        value: "1"
